/* let arr = [1, 2, 3];
let str = arr.join(",");
// let foo = () => {};
console.log(`array are ${str}`, str); */
/* function foo(arr1, arr2) {
  let cmn = [];
  let itr = arr1.length > arr2.length ? arr1.length : arr2.length;
  for (let i = 0; i < itr; i++) {
    if (arr2.includes(arr1[i])) {
      cmn.push(arr1[i]);
    }
  }
  return [...new Set(cmn)];
}
console.log(foo([1, 3, 3], [3, 3, 5]));
 */
